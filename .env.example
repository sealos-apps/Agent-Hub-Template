REGISTRY=agent-hub
AGENT_NAME=hermes-agent
AGENT_TAG=dev
AGENT_PORT=8642
AGENT_LOG_LEVEL=info
BASE_IMAGE=ghcr.io/gitlayzer/ubuntu:22.04-base
BASE_PLATFORM=linux/amd64

# Do not put runtime secrets in this file. Use environment variables,
# Kubernetes Secret, or a mounted agent-specific config file.
